---
phase: 01-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/velocity-logo.png
  - src/components/pdf/scorecard-pdf.tsx
  - src/pages/charts.tsx
  - src/lib/chart-utils.ts
  - src/lib/pdf-data-mapper.ts
  - src/lib/scoring.ts
  - src/components/pdf/pdf-styles.ts
autonomous: false

must_haves:
  truths:
    - "Velocity logo file exists in public folder"
    - "PDF export shows Velocity logo instead of UBT logo"
    - "Charts page shows Velocity logo"
    - "All code comments reference Chester, not UBT"
  artifacts:
    - path: "public/velocity-logo.png"
      provides: "Velocity logo asset"
    - path: "src/components/pdf/scorecard-pdf.tsx"
      provides: "PDF with Velocity logo and strapline"
      contains: "velocityLogoBase64"
    - path: "src/pages/charts.tsx"
      provides: "Charts page with Velocity logo"
      contains: "velocity-logo.png"
  key_links:
    - from: "src/pages/charts.tsx"
      to: "/velocity-logo.png"
      via: "img src attribute"
      pattern: 'src="/velocity-logo.png"'
    - from: "src/components/pdf/scorecard-pdf.tsx"
      to: "velocityLogoBase64"
      via: "Image component src"
      pattern: "velocityLogo"
---

<objective>
Replace UBT branding with Velocity branding in core assets and PDF generation.

Purpose: The Chester Business Scorecard needs to display Velocity branding (logo and strapline) for credibility at the Friday demo. This plan handles the foundational asset work and PDF/charts logo replacement.

Output: Velocity logo in public folder, PDF generating with new logo and strapline, charts page showing new logo, code comments updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-branding/01-RESEARCH.md
@src/components/pdf/scorecard-pdf.tsx
@src/pages/charts.tsx
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Extract Velocity logo from PowerPoint</name>
  <action>
    The Velocity logo must be extracted from the PowerPoint file before Claude can continue.
  </action>
  <instructions>
    1. Open `Docs/Chester 2026 Networking Slidedeck.pptx`
    2. Find the Velocity logo (likely on title slide or header)
    3. Right-click the logo and select "Save as Picture"
    4. Save as `velocity-logo.png` in the `public/` folder
    5. Recommended dimensions: ~400px wide for web display
    6. Note the strapline from PowerPoint: "Doing good by doing well"
  </instructions>
  <resume-signal>Type "logo extracted" when the file is saved to public/velocity-logo.png</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update PDF generation with Velocity branding</name>
  <files>src/components/pdf/scorecard-pdf.tsx</files>
  <action>
    Replace UBT logo with Velocity logo in PDF generation:

    1. Read the extracted `public/velocity-logo.png` file
    2. Convert to base64 string using PowerShell:
       `[Convert]::ToBase64String([IO.File]::ReadAllBytes("public/velocity-logo.png"))`
    3. In `scorecard-pdf.tsx`:
       - Replace line 14: `const ubtLogoBase64 = '...'` with `const velocityLogoBase64 = 'data:image/png;base64,...'` (full base64 string)
       - Replace line 15: `const ubtLogo = {...}` with `const velocityLogo = { data: velocityLogoBase64, format: 'png' }`
       - Update line 193: Change `src={ubtLogo}` to `src={velocityLogo}`
    4. Add strapline to PDF header after the title (line ~195):
       ```tsx
       <Text style={styles.headerTitle}>Chester Business Scorecard</Text>
       <Text style={{ fontSize: 7, color: '#666', marginTop: 2 }}>Doing good by doing well</Text>
       ```
  </action>
  <verify>
    - Build succeeds: `npm run build`
    - No TypeScript errors referencing ubtLogo
    - Grep for "ubt" returns no matches in scorecard-pdf.tsx: `grep -i "ubt" src/components/pdf/scorecard-pdf.tsx`
  </verify>
  <done>PDF generation uses Velocity logo and displays strapline in header</done>
</task>

<task type="auto">
  <name>Task 3: Update charts page and code comments</name>
  <files>
    src/pages/charts.tsx
    src/lib/chart-utils.ts
    src/lib/pdf-data-mapper.ts
    src/lib/scoring.ts
    src/components/pdf/pdf-styles.ts
  </files>
  <action>
    1. In `charts.tsx` (lines 107-109):
       - Change `src="/ubt-logo.png"` to `src="/velocity-logo.png"`
       - Change `alt="Universal Business Team"` to `alt="Velocity"`

    2. Update code comments in 4 files - change "UBT Monthly Business Scorecard" to "Chester Business Scorecard":
       - `src/lib/chart-utils.ts` line 2
       - `src/lib/pdf-data-mapper.ts` line 2
       - `src/lib/scoring.ts` line 2
       - `src/components/pdf/pdf-styles.ts` line 2
  </action>
  <verify>
    - Grep for "UBT" returns no matches: `grep -ri "UBT" src/`
    - Grep for "Universal Business Team" returns no matches: `grep -ri "Universal Business Team" src/`
    - Charts page loads without 404: `npm run dev` and visit /business/{id}/charts
  </verify>
  <done>Charts page shows Velocity logo, all code comments reference Chester</done>
</task>

</tasks>

<verification>
Run these checks after all tasks complete:

1. **Asset exists:**
   ```bash
   test -f public/velocity-logo.png && echo "Logo exists" || echo "MISSING"
   ```

2. **No UBT references remain:**
   ```bash
   grep -ri "UBT\|Universal Business Team" src/ --include="*.ts" --include="*.tsx"
   # Should return empty
   ```

3. **Build succeeds:**
   ```bash
   npm run build
   ```

4. **Visual verification:**
   - Run `npm run dev`
   - Navigate to charts page for any business
   - Generate a PDF export and verify logo appears
</verification>

<success_criteria>
1. `public/velocity-logo.png` exists and is a valid PNG
2. PDF exports show Velocity logo and "Doing good by doing well" strapline
3. Charts page displays Velocity logo
4. Zero references to "UBT" or "Universal Business Team" in source code
5. Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-branding/01-01-SUMMARY.md`
</output>
